# Core framework + API
sqlalchemy>=2.0
psycopg[binary]>=3.1      # PostgreSQL driver (Supabase). Omit if using SQLite.
requests>=2.31
python-dotenv>=1.0
scikit-learn>=1.4         # isotonic probability calibration (scripts/calibrate.py)
joblib>=1.3                # serializes the fitted calibrator

# Phase 1+ (uncomment when building the API and web)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
alembic>=1.13
